extends ../layouts/master

block title
    |#{title}

block styles
    link(rel='stylesheet', href='/stylesheets/forms.css')

block content
    header
        h Faculty Home
    section
        form#faculty-home(action='/project/form', role='form')
            form
                input.btn.btn-success(type='submit', value='Create New Application')